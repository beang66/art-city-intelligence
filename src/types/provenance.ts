/**
 * `observed` — transcribed from a published source listed in features/evidence/sources.
 * `illustrative` — generated for the demo; carries no empirical claim.
 */
export type Provenance = 'observed' | 'illustrative'
